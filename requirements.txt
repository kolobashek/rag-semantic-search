# RAG Catalog — зависимости
# Установка: pip install -r requirements.txt

# Интерфейсы
streamlit>=1.28.0
nicegui>=3.10.0
PyQt6>=6.5.0

# Векторная БД и эмбеддинги
qdrant-client>=1.7.0
sentence-transformers>=2.3.0

# Парсинг документов
python-docx>=1.1.0
openpyxl>=3.1.2
xlrd>=2.0.1          # чтение старого формата .xls
pdfplumber>=0.10.0

# Утилиты
tqdm>=4.66.0
requests>=2.32.5,<3
urllib3>=1.21.1,<3
charset-normalizer>=2,<4
chardet>=3.0.2,<6

# ─────────────────────────────────────────────────────────────────────
# OCR для сканированных PDF (без текстового слоя)
# ─────────────────────────────────────────────────────────────────────
# Шаг 1: установить Python-библиотеки
#   pip install pytesseract pdf2image
#
# Шаг 2: установить Tesseract (движок OCR)
#   Установщик для Windows (с русским языком):
#   https://github.com/UB-Mannheim/tesseract/wiki
#   После установки добавить папку tesseract.exe в PATH.
#
# Шаг 3: установить Poppler (конвертация страниц PDF в изображения)
#   Вариант A — через Chocolatey (требует прав администратора):
#     choco install poppler
#   Вариант B — portable, без прав:
#     https://github.com/oschwartz10612/poppler-windows/releases
#     Распаковать и добавить \Library\bin в PATH.
#
# Проверка: tesseract --version  &&  pdftoppm -v
#
pytesseract>=0.3.10
pdf2image>=1.16.3
pytest>=8.0.0
